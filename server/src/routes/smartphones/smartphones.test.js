describe('Test GET /api/v1/smartphones', () => {
  test('It should response with 200 status code', () => {
    const response = 200;
    expect(response).toBe(200);
  });
});

describe('Test POST /api/v1/smartphones', () => {
  test('It should created a new smartphone with 201 status code', () => { });
});
